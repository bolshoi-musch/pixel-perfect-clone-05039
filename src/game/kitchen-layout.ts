// Фиксированные позиции объектов кухни. PRD: 5 слотов хранения + 1 рабочая зона.
// Расширяемо для будущих устройств (тостер, блендер, рисоварка).

export type Vec3 = [number, number, number];

/** Слоты для хранения продуктов на столе (передний ряд). */
export const TABLE_SLOT_POSITIONS: Vec3[] = [
  [-1.6, 0.06, 0.35],
  [-0.8, 0.06, 0.35],
  [0, 0.06, 0.35],
  [0.8, 0.06, 0.35],
  [1.6, 0.06, 0.35],
];

/** Единственная рабочая зона. */
export const WORK_SURFACE_POSITION: Vec3 = [0, 0.06, -0.35];

/** Позиции стационарных устройств (на столе/у стены). */
export const EQUIPMENT_POSITIONS: Record<string, Vec3> = {
  stove: [-1.7, 0.2, -0.4],
  kettle: [-1.0, 0.18, -0.4],
  bowl: [-0.7, 0.06, -0.35],
  plate: [0.5, 0.06, -0.35],
  cup: [1.3, 0.1, -0.35],
  bell: [1.55, 0.08, 0.2],
  // Будущая техника (auto-place when bought):
  toaster: [-0.3, 0.18, -0.4],
  blender: [0.3, 0.22, -0.4],
  rice_cooker: [1.0, 0.2, -0.4],
};
